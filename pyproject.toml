[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "omero-analysis"
version = "0.9.4"
description = "Browser-local Chat and run-only Notebook analysis for OMERO.web"
readme = "README.md"
requires-python = ">=3.10"
license = "AGPL-3.0-or-later"
authors = [{ name = "NL-BioImaging" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Framework :: Django",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = [
  "omero-web>=5.6.0",
  "packaging>=23",
]

[project.optional-dependencies]
biomero-skills = [
  "biomero-workflow-skills>=0.3,<0.4",
]
test = [
  "Django>=3.2,<6",
  "pytest>=8",
  "pytest-django>=4.8",
  "build>=1.2",
]

[project.urls]
Homepage = "https://github.com/NL-BioImaging/OMERO.Analysis"
Repository = "https://github.com/NL-BioImaging/OMERO.Analysis"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
omero_analysis = [
  "templates/**/*.html",
  "static/**/*",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
